import CertificateEditorLayout from './CertificateEditorLayout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <CertificateEditorLayout>{children}</CertificateEditorLayout>;
}
